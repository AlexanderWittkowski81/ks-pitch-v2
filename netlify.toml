# Netlify Config - Static Site
# index.html ist vor-gebaut im Repo. Für Updates: lokal python3 build.py laufen lassen, dann committen.
[build]
  publish = "."
  # Build-Command absichtlich leer - index.html wird direkt serviert
